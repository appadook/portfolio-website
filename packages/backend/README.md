# @portfolio/backend

App-agnostic Convex backend package containing schema and function modules.

## Scripts

- `bun run dev` starts Convex development.
- `bun run deploy` deploys backend functions.
- `bun run codegen` generates typed client references.

Run from repo root with Turbo:

- `bun run dev:convex`
